import React from 'react';
import CustomersPage from '@/components/CustomersPage';

const page = () => {
    return (
        <div>
            <CustomersPage />
        </div>
    );
};

export default page;